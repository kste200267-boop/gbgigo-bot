/* =====================================================================
   AI(하이브리드) 설정
   - 규칙기반(50여 문항)으로 못 찾을 때만 Cloudflare Workers AI가 답해요.
   - AI 한도 초과·오류 시 자동으로 규칙기반 안내로 돌아갑니다(폴백).
   ---------------------------------------------------------------------
   ▶ Cloudflare Worker 배포 후, 아래에 Worker 주소를 넣으면 AI가 켜져요.
     예) window.AI_WORKER_URL = "https://gbgigo-ai.<계정>.workers.dev";
   ▶ 비워두면("") AI 없이 규칙기반만 동작합니다(지금 상태).
   ===================================================================== */
window.AI_WORKER_URL = "https://gbgigo-ai.gbgg-bot.workers.dev";
